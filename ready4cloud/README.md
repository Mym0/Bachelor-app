# Bachelor-app
Ready4Cloud Webapp ist eine Schnittstelle, die bei der Entscheidungshilfe für die Fragestellung, ob ein Service On Premise oder in der Cloud bereitgestellt werden sollte

##Installation
Open the folder in you IDE and run the following command in the Terminal
`npm install`

##Run the Project
After installing the node Packages you can run the Project using the following command
`npm run start`